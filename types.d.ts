export type AppEvent = {
    id: string
    name: string
    date: string
    attendants: string[]
}
export type User = {
    id: string
    name: string
    surname: string
    email: string
    residance: string
}